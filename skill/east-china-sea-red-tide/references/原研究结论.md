# 原研究结论索引

以下指标属于原项目实验记录，不能作为新技能下一月模型的指标。现有材料可能包含不同论文或实验版本，本文件仅引用已核对的报告。

| 原始文件 | 记录内容 | 解释限制 |
|---|---|---|
| output_lightgbm_v3/model_report.txt | 46,800 条样本、17,355 个正例；空间留出 AUC 0.9559、F1 0.8464 | 使用旧版区域统一标签；输入与标签同月；不是下月预测测试 |
| output_stacking/stacking_model_report.txt | 2020—2023 年 LightGBM AUC 0.6982；Stacking-LGB AUC 0.6982、F1 0.5833 | 与上一个报告属于不同样本与流程；不能混合比较 |
| output_stacking/meta_features_report.txt | 240 个月：168 训练、24 验证、48 测试 | 元特征构建含随机折和缺失模型输出替代，详见审计 |

上述报告的原文副本将保存在 `legacy/reports/`。问答优先给出报告名和实验口径，不能只取最高 AUC。新下一月模型指标读取 `models/regional_v1/evaluation.json`。
