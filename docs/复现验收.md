# 复现验收

## 已执行

- 2026-09-15 已公开发布至 `Hu-yoouk/east-china-sea-red-tide`。
- GitHub Actions 的 Windows、Ubuntu 与 macOS 托管环境均完成固定依赖安装、7 项测试、历史样例推理和重训概率一致性检查，全部通过。
- [自动验证记录](https://github.com/Hu-yoouk/east-china-sea-red-tide/actions/runs/34939570954)，对应代码提交 `24073dee9be6f4ea3a72a34b9262cdc024257c0a`；后续发布整理仅更新 README 和本验收文档。
- 交付 ZIP 已解压到独立目录，逐文件 SHA256 校验、测试、推理和历史查询通过。Git 使用原始字节保存文件，避免跨系统换行转换改变数据与模型校验值。

- Windows、Python 3.12 独立环境安装固定依赖与项目成功。
- 两个数据库只读审计完成，原文件未修改。
- 模型按 156 / 24 / 48 个月训练、验证、测试成功。
- 权重重新加载后推理与独立测试结果一致。
- 第二次重训的测试日期、标签和概率一致，绝对误差不超过 1e-8。
- 程序测试覆盖下一月对齐、目标月数据不进入特征、预测不读取标签、缺字段、缺月、重复月、气压单位、数据库只读和样本数。
- 两个独立 Agent 在有技能与无技能条件下均得到 0.17220336585862192，均拒绝缺少气压输入。
- 两个 Agent 均说明不能给出明天舟山逐网格概率，没有伪造预测。
- 历史查询返回 2021 年 8 / 12 月阳性；2023 年输入可预测 2024-01 并提示超出测试期。
- 本地浏览器已检查预测报告的中文、指标、模型贡献和文件链接。
- 历史网站使用锁定依赖完成 TypeScript 检查与 Vite 生产构建；保留原有大分块体积提示。

简单案例中两种条件均通过，不能据此宣称技能显著提升模型能力。本次验证的是调用约定可由其他 Agent 执行。

## 已修复

Windows 中文绝对路径经 LightGBM 文件接口加载失败，改为 Python 读文本后传入模型，测试已覆盖。终端示例设 UTF-8，输出文件统一 UTF-8。

Windows 云端终端默认编码导致复现检查最后的中文成功提示报错，已为该脚本标准输出显式设置 UTF-8，并在 CI 中设置 `PYTHONUTF8=1`；修复后的三系统自动验证通过。

## 未执行

- Linux/macOS 用户个人电脑安装测试；已执行的是 GitHub 托管环境测试。
- 其他品牌 Agent 平台实测；本次仅使用当前环境独立 Agent。
- Docker 网站部署和真实业务预警试运行。

跨系统 CI 的实际结果见上方链接。Skill Creator 本地缺少其引用的评估查看器和 schema 文件，使用实际命令日志、结果 JSON 和核对表记录验收，未运行缺失工具。
